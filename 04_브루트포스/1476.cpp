#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
// 범위 설정 (1 ≤ E ≤ 15, 1 ≤ S ≤ 28, 1 ≤ M ≤ 19)

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    //입력 (지구, 태양, 달)
    int e, s, m;
    cin >> e >> s >> m;

    //연산
    //출력

}